{% extends "_layouts/_wrapper.twig" %}
{% block main %}
{% set testimonials = craft.entries.section('testimonials').limit(2).orderBy('RAND()').collect() %}
    <main>
      <div>
        <div class="relative">
        {# Hero Section for Homepage #}
          <div class="max-w-full">
            <div class="relative sm:overflow-hidden">
              <div class="absolute inset-0">
                <img
                  class="h-full w-full object-cover"
                  src="{{ entry.image.one.url }}"
                  alt="{{ entry.title }}"
                />
                <div
                  class="absolute inset-0 bg-gray-300 mix-blend-multiply"
                ></div>
              </div>
              <div
                class="relative px-4 py-16 sm:px-6 sm:py-24 lg:py-32 lg:px-8"
              >
                <h1
                  class="text-center text-5xl font-extrabold tracking-tight md:5xl sm:text-7xl lg:text-7xl"
                >
                {% set headline = entry.headline | split(' ')%}
                {% for words in headline | batch(4)%}
                  <span class="block text-white">
                    {% for word in words %}
                      {{ word }}
                    {% endfor %}
                  </span>
                {% endfor %}
                </h1>
                <p
                  class="mt-6 max-w-lg mx-auto text-center text-xl text-white sm:max-w-3xl"
                >
                  {{entry.tagline}}
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
      {% include '_shared/_adventures-listing' %}
      <section class="overflow-hidden md:py-20 mt-20 bg-white">
        <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                  <div class="relative">
            <blockquote class="">
              <div
                class="max-w-3xl mx-auto text-center text-4xl leading-9 font-bold text-secondary"
              >
                <p>
                  &ldquo;{{ testimonials.first().testimonialCopy }}&rdquo;
                </p>
              </div>
              <footer class="my-8">
                <div class="md:flex md:items-center md:justify-center">
                  <div
                    class="mt-3 text-center md:mt-0 md:ml-4 md:flex md:items-center"
                  >
                    <div class="text-base font-medium text-gray-900">
                      {{ testimonials.first().testimonialByline[0].fullName }} &nbsp;
                    </div>
                    <div class="text-base font-medium text-gray-500">
                      {{ testimonials.first().testimonialByline[0].location }}
                    </div>
                  </div>
                </div>
              </footer>
            </blockquote>
          </div>
        </div>
      </section>
      <section class="bg-white">
        {% include '_shared/_locations' %}
      </section>
      <section class="overflow-hidden md:py-20 lg:py-20 bg-white">
        <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                    <div class="relative">
            <blockquote class="">
              <div
                class="max-w-3xl mx-auto text-center text-4xl leading-9 font-bold text-secondary"
              >
                <p>
                  &ldquo;{{ testimonials.last().testimonialCopy }}&rdquo;
                </p>
              </div>
              <footer class="mt-8">
                <div class="md:flex md:items-center md:justify-center">
                  <div
                    class="mt-3 text-center md:mt-0 md:ml-4 md:flex md:items-center"
                  >
                    <div class="text-base font-medium text-gray-900">
                      {{ testimonials.last().testimonialByline[0].fullName }} &nbsp;
                    </div>
                    <div class="text-base font-medium text-gray-500">
                      {{ testimonials.last().testimonialByline[0].location }}
                    </div>
                  </div>
                </div>
              </footer>
            </blockquote>
          </div>
        </div>
      </section>
    </main>
  {% endblock %}    






